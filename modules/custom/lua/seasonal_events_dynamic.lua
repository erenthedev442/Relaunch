-- seasonal_events_dynamic.lua
-- Written at runtime by the !setbonus GM command.
-- Delete this file (or let it expire) to clear a manually-triggered event.
-- seasonal_events.lua reads this file via loadfile (not require), so it
-- picks up changes within 60 seconds without a server restart.
return {}
