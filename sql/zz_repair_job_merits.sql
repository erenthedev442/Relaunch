-- =============================================================================
-- zz_repair_job_merits.sql  --  NEUTRALIZED 2026-06-21 (was destructive)
--
-- !!! DO NOT PUT A `DELETE` / one-time data migration IN A zz_*.sql FILE !!!
--
-- ORIGINAL PURPOSE (one-time): refund + DELETE every non-generic merit row
-- (job-specific + Others, meritid >= 320 / 0x0140) to undo damage from the OLD
-- category-unfiltered !automerits command.
--
-- THE BUG IT CAUSED: this file lives in sql/zz_*.sql, and on the Azure box
-- tools/_apply_changed_sql.sh takes its non-git fallback path, which applies
-- EVERY sql/zz_*.sql UNCONDITIONALLY on EVERY deploy/restart (the zz_* set has
-- no change-ledger -- it is designed for IDEMPOTENT full-table gear/mod
-- replacements, NOT one-time data migrations). So the `DELETE FROM char_merit
-- WHERE meritid >= 320` re-ran on every server restart and wiped players'
-- job-specific + Others merit allocations every time -- the reported "merits
-- revert to zero on restart." (Step 2 refunded the spent points each time, so
-- balances survived; only the allocations were lost, and players could
-- re-allocate until the next deploy wiped them again.)
--
-- FIX: emptied to a NO-OP so deploys stop wiping merits. The active
-- automerits.lua already has the correct category filter (0x0040..0x0100 only),
-- so the original old-automerits damage cannot recur and no repair is needed.
--
-- If a one-time merit migration is ever needed again, run it BY HAND once
-- (mysql xidb < file.sql) -- never as a zz_*.sql. The previous destructive
-- contents are in git history (before this commit) if ever required.
-- =============================================================================

-- Intentionally a no-op. Do not add data-modifying statements here.
DO 1;
