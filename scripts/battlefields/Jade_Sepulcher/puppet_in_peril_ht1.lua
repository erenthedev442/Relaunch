-----------------------------------
-- Area: Jade Sepulcher
-- High-Tier Mission Battlefield: Puppet in Peril -- Tier I
-- Reuses the base fight (reuseBaseId) via the HTBF registrar; uses arenas 2-3 so
-- it never blocks the base mission (arena 1).
-----------------------------------
return require('modules/custom/lua/htbf').register('puppet_in_peril', 1)
